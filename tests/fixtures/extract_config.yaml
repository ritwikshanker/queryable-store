# Same identities as merge_config.yaml, plus llm.chat_model/embedding_model set
# so `chatmem extract`/`chatmem query` don't fail their config checks. Tests
# still stub out the actual LLM client -- no real model is ever called.
target: target

identities:
  - id: target
    display_name: Alex Rivera
    aliases: ["Alex Rivera", "Alex R."]
  - id: other
    display_name: Sam Chen
    aliases: ["Sam Chen"]

llm:
  chat_model: "test-model"
  embedding_model: "test-embed-model"
